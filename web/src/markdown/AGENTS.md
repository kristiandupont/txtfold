# markdown

Self-contained Markdown renderer used by the web UI.

- `Markdown.tsx` — Crank component; renders a `content: string` prop to HTML via `marked`
- `markdown.css` — styles for the rendered output (GitHub-style)
